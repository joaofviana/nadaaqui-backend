# Plano de correção — gaps NadaAqui

Ver execução nas PRs `fix/gaps-f1-f8` (backend + mobile).
Ordem original: F0 unificar PRs (feito no mobile main) → F1 contrato → F2 RLS → F3 cron → F4 testes → F5 auth → F6 live → F7 smoke → F8 CI.
